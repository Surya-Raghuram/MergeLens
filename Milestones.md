[ ] **Milestone 1**: The Foundation. Setup Docker Compose, the backend scaffolding (FastAPI + Redis + Celery/Arq), and the Supabase connection.

[ ] **Milestone 2**: The GitHub Webhook & Pipeline. Create the GitHub App securely, write the webhook receiver, and implement the background worker pipeline.

[ ] **Milestone 3**: The C++ Analyzer. Write the C++ tool using CMake, parse a simple code structure, output JSON, and write the Python wrapper to execute it.

[ ] **Milestone 4**: The LangGraph Brain. Implement the agent state, the routing logic (detecting languages), the specific reviewers, and the verification step.

[ ] **Milestone 5**: The GitHub Publisher. Authenticate as a GitHub App installation, map agent feedback to specific code lines, and publish inline review comments.

[ ] **Milestone 6**: The Flutter Dashboard. Build the frontend with Supabase Auth, connect it to the database for real-time logs, and create the configuration UI.