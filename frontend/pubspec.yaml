name: frontend
description: "MergeLens Engineering Control Center Dashboard"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  google_fonts: ^6.1.0
  url_launcher: ^6.2.6

flutter:
  uses-material-design: true